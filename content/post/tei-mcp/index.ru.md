---
title: "tei-mcp: TEI P5 для ИИ-агентов"
subtitle: MCP-сервер, который помогает ИИ-ассистентам разбираться в Руководстве TEI

summary: >
  tei-mcp — MCP-сервер с открытым исходным кодом, дающий ИИ-ассистентам для
  программирования прямой доступ к спецификации TEI P5 — поиск элементов, разрешение
  атрибутов, проверка вложенности, валидация документов и ODD-настройка.

date: "2026-03-15T00:00:00Z"
lastmod: "2026-03-15T00:00:00Z"

draft: false
featured: true
machine_translated: true

authors:
- clement

tags:
- Цифровые гуманитарные науки
- TEI
- MCP
- ИИ

categories:
- Цифровые гуманитарные науки
---

Если вы когда-нибудь писали TEI XML с помощью ИИ-ассистента для программирования, 
вы наверняка замечали, что он ошибается. Элементы появляются там, где им не место. 
Атрибуты выдумываются. Правила вложенности игнорируются. Модель примерно представляет, 
как выглядит TEI, но надёжного знания спецификации у неё нет.

tei-mcp решает эту проблему, давая ИИ-агентам прямой доступ к Руководству TEI P5 
через инструменты.

{{< toc >}}

## Что такое MCP?

[Model Context Protocol](https://modelcontextprotocol.io) (MCP) — открытый стандарт, 
позволяющий ИИ-приложениям подключаться к внешним источникам данных и инструментам. 
Считайте его USB-портом для ИИ: единый протокол, через который любой совместимый 
клиент — Claude, Cursor, Windsurf и другие — подключается к специализированным службам.

MCP-сервер предоставляет *инструменты*, которые ИИ может вызывать по ходу разговора. 
Вместо того чтобы полагаться на заученные обучающие данные, модель обращается к живому 
авторитетному источнику.

## Что делает tei-mcp

tei-mcp разбирает спецификацию TEI P5 в формате ODD и предоставляет 16 инструментов, 
покрывающих самые частые вопросы редактора или кодировщика:

- **Что это за элемент?** Поиск любого элемента, класса, макроса или модуля по имени, 
  без учёта регистра и с подсказками при опечатках.
- **Какие у него атрибуты?** Разрешение атрибутов по всей иерархии классов: сначала 
  локальные, затем унаследованные по порядку.
- **Что может быть внутри?** Развёртывание моделей содержимого в структурированные 
  деревья или плоский список допустимых потомков.
- **Можно ли поставить элемент сюда?** Проверка вложенности «родитель — потомок» 
  или трассировка достижимости по всей иерархии элементов.
- **Валиден ли мой документ?** Проверка файла TEI XML по спецификации: модели 
  содержимого, значения атрибутов, закрытые списки значений, целостность ссылок 
  и предупреждения об устаревших элементах.
- **А как же схема моего проекта?** Загрузка файла ODD-настройки, чтобы ограничить 
  всё перечисленное подмножеством TEI, принятым в вашем проекте.

## Почему это важно

Кодирование в TEI требует постоянно сверяться с Руководством. Опытные кодировщики 
усваивают самые ходовые схемы, но даже им приходится заглядывать в спецификацию ради 
менее знакомых элементов или сложных моделей содержимого. У ИИ-ассистентов такого 
усвоенного знания нет, и им приходится хуже: они галлюцинируют правдоподобную на вид, 
но неверную разметку.

С tei-mcp ИИ не приходится гадать. Он может найти ответ в спецификации, прежде чем 
напишет хоть одну угловую скобку. В результате разметка соответствует TEI P5 — или 
ODD-настройке вашего проекта.

## С чего начать

Установите из PyPI:

```bash
pip install tei-mcp
```

Затем добавьте сервер в конфигурацию вашего MCP-клиента:

```json
{
  "mcpServers": {
    "tei": {
      "command": "uvx",
      "args": ["tei-mcp"]
    }
  }
}
```

При первом запуске сервер скачивает спецификацию TEI и работает с любым 
MCP-совместимым клиентом.

Исходный код и полная документация: 
[github.com/Pantagrueliste/tei-mcp](https://github.com/Pantagrueliste/tei-mcp)
